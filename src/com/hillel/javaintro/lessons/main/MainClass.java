package com.hillel.javaintro.lessons.main;

import static com.hillel.javaintro.lessons.test.TestClass.*;
import java.util.Calendar;
import java.util.GregorianCalendar;

public class MainClass {
    public static void main(String[] args) {
        /*String text ="Hello 67565  7644 8 5 0hello53";
        System.out.println(text);
        String t = removingNumFromString(text);
        System.out.println(t);
        String p = removingSpaceFromSentence(text);
        System.out.println(p);
        text ="Hello fhdlf dfhlsd   dsfsd";
        int c = countOfTheWordsInSentense(text);
        System.out.println(c);
        final int num = 974386260;
        boolean b = checkingTelNum(num);
        System.out.println(b);

        text ="Hello";
        AddTask.AllLater w ;
        w = countAllLater(text);
        System.out.print(text+": ");
        for (int i = 0; i < w.counterNonNull; i++) {
            System.out.print(w.later[i]);
            System.out.print(w.counter[i]);
        }*/
        /*int[] input = new int[]{1, 1, 1, 2, 2, 1, 2};
        int i = 1, j = 0;
        Arrays.sort(input);
        while (i < input.length) {
            if (input[i] == input[j]) {
                i++;
            } else if (!(input[i] == input[j])){
                input[++j] = input[i++];
            }
        }
        int[] output = new int[j + 1];
        for (int k = 0; k < output.length; k++)
            output[k] = input[k];

            for (i = 0; i < input.length; i++)
                System.out.println(output[i]);


        */

        int[]n = new int[]{3,4,2,3};
        System.out.println();

    }
}
